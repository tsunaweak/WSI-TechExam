$(document).ready(function () {
    $(document).on('click', '#btn_search_product_id', function () {
        let id = $('#search_product_id').val();
        if (id.length > 0) {
            
        }
    });
});